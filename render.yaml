services:
  # ── Backend: FastAPI ────────────────────────────────────────────────────────
  - type: web
    name: vaxagent-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: USE_FIXTURES
        value: "true"
      - key: CORS_ORIGIN
        value: "*"
      - key: ESMFOLD_API_URL
        value: "https://api.esmatlas.com/foldSequence/v1/pdb/"
      - key: ALPHAFOLD_API_BASE
        value: "https://alphafold.ebi.ac.uk/api"
      - key: MHC_BINDING_THRESHOLD_NM
        value: "500"
      - key: ANTHROPIC_API_KEY
        sync: false   # fill this in the Render dashboard (optional)

  # ── Frontend: Static Site ───────────────────────────────────────────────────
  - type: web
    name: vaxagent-frontend
    runtime: static
    staticPublishPath: frontend
    buildCommand: ""
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /index.html
        destination: /index.html
